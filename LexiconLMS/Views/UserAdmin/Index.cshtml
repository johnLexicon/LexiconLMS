@model IEnumerable<LexiconLMS.Models.User>

@{
    ViewData["Title"] = "Teacher Accounts";
}

<h1>@ViewData["Title"]</h1>
<hr />

<table class="table tablestyle">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FullName)
            </th>
            <th>@Html.DisplayNameFor(model => model.Email)</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FullName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-purple btn-sm">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-green btn-sm">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-grey btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<hr />
<a asp-action="Create" class="btn btn-green">Create Teacher Account</a>